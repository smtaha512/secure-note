import { SecureNoteTypeormEntity } from './secure-note.typeorm.entity';

describe('SecureNoteTypeormEntity', () => {
  it('should be defined', () => {
    expect(new SecureNoteTypeormEntity()).toBeDefined();
  });
});
